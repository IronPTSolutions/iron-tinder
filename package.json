{
  "name": "iron-tinder",
  "version": "1.0.0",
  "private": true,
  "description": "Iron Tinder - Full Stack Dating App Lab",
  "scripts": {
    "api": "npm run dev --prefix api",
    "web": "npm run dev --prefix web"
  }
}
